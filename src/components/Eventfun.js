import React from "react";

const Eventfun = () => {
  const getElement = () => {};

  return (
    <div>
      <h2>events in react</h2>
      <button onClick={getElement}>click to get current element</button>
    </div>
  );
};

export default Eventfun;
